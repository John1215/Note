<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/RecyclerviewBG"
    xmlns:toolbar="http://schemas.android.com/tools">
    <!--toolbar-->
    <android.support.v7.widget.Toolbar xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="20dp"
        android:titleTextColor="@color/colorWhite"
        android:elevation="2dp"
        android:background="@color/colorPrimary"
        app:navigationIcon="@drawable/ic_btn_slide_bar"
        app:title="@string/toolbar_title"
        app:titleTextColor="@color/colorWhite"
        app:theme="@style/OverFlowMenuTheme"
        >
        <TextView
            android:id="@+id/toolbar_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_gravity="center"
            android:text="@string/toolbar_title"
            android:textColor="@color/colorWhite"
            android:textSize="24dp"

            />
    </android.support.v7.widget.Toolbar>
    <!--swiperefresh-->
    <android.support.v4.widget.SwipeRefreshLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
        android:layout_marginTop="75dp"
    android:id="@+id/swiperefreshlayout">
    <!--RecycleView-->
        <com.android.john.note.RecyclerViewSwipe
            android:id="@+id/recycleview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"></com.android.john.note.RecyclerViewSwipe>
    </android.support.v4.widget.SwipeRefreshLayout>

    <include layout="@layout/fab_plus"/>


</android.support.design.widget.CoordinatorLayout>